import "./checkout.styles.scss";

const Checkout = () => {
  // Checkout Page, different checkout items
  // Increment the item and decrement the item using arrow
  // Cross button to remove an item entirely
  // Comprise the total value
};

export default Checkout;
